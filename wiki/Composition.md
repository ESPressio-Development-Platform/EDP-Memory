# Composition

The Memory domain exposes ByteOperations, MemoryResource and SharedReserveAllocationAlgorithm capabilities. A topology is an application compile-time plan over concrete providers selected and owned by Bootstrap.

Provider capability/property qualification determines which concrete resources may satisfy the plan; runtime selection never becomes an unbounded allocator registry.
